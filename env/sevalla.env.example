# Optional Sevalla automation (copy to env/sevalla.env — never commit)
# Revoke and rotate keys if exposed in chat or logs.

SEVALLA_API_KEY=

# After deploy, set in env/nwrma.env (or use set-public-api-url.mjs):
# NWRMA_API_URL=https://your-api.sevalla.app
# PUBLIC_APP_URL=https://your-web.sevalla.app
